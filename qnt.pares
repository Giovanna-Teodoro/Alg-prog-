def verifica_par(num):
    if num%2==0:
        return True
    else:
        return False
    

x,y=map(int,input().split())

cont=0
i=x
while i>= x and i<y:
    par=verifica_par(i)
    if par==True:
        cont+=1
        
    i+=1


print(cont)

